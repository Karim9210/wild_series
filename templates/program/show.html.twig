{% extends 'base.html.twig' %}


{% block body %}
	<h1>
		<a href="{{ path('program_id', {id}) }}">"Program 4"</a>
	</h1>
	<ul>
		{% for p in program %}

			<li>{{ p.title }}</li>
		{% endfor %}
	</ul>

{% endblock %}
